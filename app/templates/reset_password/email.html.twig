{#<h1>Hi!</h1>#}

{#<p>Pour reinitialiser votre mot de passe, veuillez cliquer sur le lien suivant.</p>#}

{#<a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a>#}

{#<p>Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>#}

{#<p>Cheers!</p>#}

<!doctype html>
<html lang="en">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <meta name="viewport" content="width=device-width">
    <title>{{ email.subject }}</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #f3f3f3;
            font-family: Helvetica, Arial, sans-serif;
        }
        h1 {
            background-color: #264459;
            color: #ffffff;
            padding: 30px 0 50px 0;
            font-weight: normal;
        }
        hr {
            border: none;
            border-top: 3px solid #264459;
            margin: 20px;
        }
        .container {
            background-color: #fefefe;
            width: 580px;
            margin: 0 auto;
        }
        .bottom {
            background-color: #efefee;
        }
        .block {
            margin: 0;
            padding: 10px 20px 20px 20px;
        }
        .logo {
            width: 100%;
        }
        .text-center {
            text-align: center;
        }
        .btn {
            display: inline-block;
            padding: 10px 20px;
            background-color: #264459;
            color: #fefefe;
            border: 1px solid #fff;
            border-radius: 3px;
            font-size: 20px;
            font-weight: bold;
            text-decoration: none;
        }
    </style>
</head>
<body>
<div class="body">
    <div class="container">
        <div class="header text-center">
            <a href="{{ url('homepage') }}">
                <img src="{{ absolute_url(asset('build/images/email/logo.png')) }}" class="logo" alt="Snowtricks Logo">
            </a>
        </div>
        <div class="content">
            <h1 class="text-center">Hi {{ email.toName }}!</h1>
            <p class="block">
                Nous sommes heureux de vous revoir sur <strong>Snowtricks</strong>
                Pour reinitialiser votre mot de passe, merci de cliquer sur le lien suivant:
            </p>
            <p class="block text-center">
                <a href="{{ url('app_reset_password', {token: resetToken.token}) }}" class="btn">Reinitialiser le mot de passe</a>
            </p>

            <p class="block">Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

            <p class="block">Si vous n'etes pas l'auteur de cette demande, ignorez ce message.</p>
        </div>
        <div class="footer">
            <p class="block">
                L'équipe <em>Snowtricks</em>
            </p>
        </div>
    </div>
</div>
</body>
</html>